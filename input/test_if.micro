PROGRAM test
BEGIN

	INT i,a,num;
	
	FUNCTION VOID main()
	BEGIN
		num := 7;
		i := 2;	
		a := 42;
	
		IF (i<5)
			num := num +a;
            IF (i >= 10)
                num := num + 69;
            FI
		ELSE
			num := num -a;
		FI

		WRITE(num);
	END
END
